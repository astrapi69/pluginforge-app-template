"""${class_name} service.

Plain functions, one per business operation. Routers stay thin and
delegate here. Errors flow through ``${pascal_name}Error`` subclasses per
``.claude/rules/code-hygiene.md``.
"""

${imports_block}

${functions_block}