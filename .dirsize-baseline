# Directory-size baseline (docs/patterns/07) - flat SOURCE-file counts > 15
# that are tolerated until split (ratchet: may only shrink, never grow; a NEW
# oversized directory fails `check-directory-size.sh --gate`). This file IS the
# god-folder whitelist. components/ is the EXAMPLE-DOMAIN demo surface and
# shrinks naturally as you replace the example domain with your own.
# Format: <path>  # reason
frontend/src/components   # EXAMPLE-DOMAIN demo components (shrinks on customization)
frontend/src/hooks        # shared hooks
